function appel_string() {
    for (i = 0; i < 6; i++) {
        var scoreEq = 'score' + i + '_eq' + num;
        var resultA = document.getElementById(ScoreEq).value;
        var scoreA = appel_total_score(resultA + i);
    }
}

var scoreA = appel_total_score(resultA + i);

function appel_total_score(num) {

    var result = document.getElementById(scoreEq).value;
}
